= link_to "&laquo; #{t('shared.back')}".html_safe, admin_places_path, class: :left

= link_to "+ #{t('shared.create') }".html_safe, new_admin_place_candidate_path(place), class: 'light-button'

= link_to "#{t 'shared.skip'} &raquo;".html_safe, next_path(current_admin_user, place), class: :right
